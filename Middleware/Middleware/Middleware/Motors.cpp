#include "Motors.h"

bool SetupMotors() 
{
	//This function will be called once by the middleware at the start of the program to initialize any global variables in this file.
	//Please add any parameters that you need here and on "Motors.h"

	return true; //true for success. False for any error
}

bool RotateMotors(Vector2 angle)
{
	//This function will be called on every cycle by the middleware to rotate the motors.
	//Please add any parameters that you need here and on "Motors.h"


	/*			Turn motors
	* TurnMotor(A, angle.x); //Example function to turn motor A by angle.x degrees
	* TurnMotor(B, angle.y);
	*/

	//true for success. False for any error
}